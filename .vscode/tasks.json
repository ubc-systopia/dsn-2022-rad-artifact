{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "stubs",
            "type": "shell",
            "command": "python -m grpc_tools.protoc -I. --python_out=. --grpc_python_out=. ./niraapad/protos/niraapad.proto"
        },
        {
            "label": "build",
            "dependsOrder": "sequence",
            "dependsOn": ["stubs"],
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "test",
            "type": "shell",
            "command": "py ./niraapad/test/test_niraapad.py"
        }
    ]
}
